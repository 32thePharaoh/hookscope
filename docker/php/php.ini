; nginx client_max_body_size is the primary cap (1m) and returns JSON 413.
; Keep PHP limits above that so PUT/PATCH never die on post_max_size first.
post_max_size = 2M
upload_max_filesize = 2M
memory_limit = 256M
expose_php = Off
