[www]
; Docker injects APP_KEY and DB_* via the process environment. The default
; clear_env = yes would strip them from FPM workers and the app would 500.
clear_env = no
ping.path = /ping
ping.response = pong
